{
  "name": "project-1-basic-ci",
  "version": "1.0.0",
  "description": "Node.js calculator API — Project 1: Basic CI",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint src/ tests/",
    "test": "jest --coverage",
    "build": "node -e \"require('./src/index.js')\" && echo 'Build check passed'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
